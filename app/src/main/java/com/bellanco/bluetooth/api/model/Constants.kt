package com.bellanco.bluetooth.api.model

object Constants {

    object Preferences {
        const val ENCRYPTED_PREFERENCES = "ENCRYPTED_PREFERENCES"

        const val KEY_VERSION_JSON = "KEY_VERSION_JSON"
    }

    object Database {
        const val DATABASE_NAME = "LocalDatabase"
    }
}